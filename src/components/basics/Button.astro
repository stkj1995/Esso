---
const { 
  textMobile, 
  textDesktop, 
  link, 
  backgroundColor, 
  textColor, 
  size, 
  additionalClasses 
} = Astro.props;
---

<!-- Button for Desktop & Mobile -->
<a href={link} 
   class={`inline-block px-6 py-3 text-white rounded-lg ${backgroundColor} ${textColor} ${size} ${additionalClasses}`}>
   
   <!-- Desktop Version -->
   <span class="hidden md:inline">{textDesktop}</span>
   
   <!-- Mobile Version -->
   <span class="md:hidden">{textMobile}</span>

</a>

<!-- Denne kode bruges på siderne til knapper (varianter) -->
<!-- <Button 
  textMobile="Tilmeld dig" 
  textDesktop="Tilmeld dig nyhedsbrev" 
  link="/nyhedsbrev" 
  backgroundColor="bg-blue-500" 
  textColor="text-white" 
  size="py-3 px-6" 
  additionalClasses="hover:bg-blue-700" 
/> -->
